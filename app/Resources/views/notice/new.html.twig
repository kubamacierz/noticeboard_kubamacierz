{% extends 'base.html.twig' %}

{% block body %}
    <h1>Stwórz nowe ogłoszenie</h1>

    {{ form_start(form) }}
    {{ form_widget(form.expiration, { 'attr': { 'readonly': 'true' } }) }}
    <input type="submit" value="Utwórz" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('notice_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
