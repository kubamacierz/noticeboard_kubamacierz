{% extends 'base.html.twig' %}

{% block body %}
    <h1>Comment creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('comments_by_notice_id', { 'id': comment.noticeId }) }}">
                Back to comments of notice no {{ comment.noticeId }}
            </a>
        </li>
    </ul>
{% endblock %}
